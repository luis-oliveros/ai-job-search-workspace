# Applications

Generated application workspaces are stored here locally and ignored by Git.

Create one with:

```bash
python scripts/new_application.py "Organization" "Role" --url "https://example.com/job"
```
